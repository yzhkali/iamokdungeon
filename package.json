{
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check:assets": "node scripts/check-assets.mjs",
    "check:vendor-subset": "node scripts/check-assets.mjs",
    "check:syntax": "node scripts/check-syntax.mjs",
    "check:server": "node scripts/check-server.mjs",
    "check:browser": "node scripts/check-browser.mjs",
    "validate": "npm run check:assets && npm run check:vendor-subset && npm run check:syntax && npm run check:server",
    "serve": "node scripts/static-server.mjs prototype/3d",
    "prepush": "git diff --check && npm run validate && npm run check:browser && test ! -e assets/packs/vendor"
  }
}
